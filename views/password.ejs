<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yêu cầu mật khẩu</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
        }
        .password-card {
            max-width: 400px;
            margin: 0 auto;
            background: rgba(255,255,255,0.95);
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.1);
        }
        .lock-icon {
            font-size: 3rem;
            color: #667eea;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card password-card">
            <div class="card-body text-center p-5">
                <i class="fas fa-lock lock-icon mb-4"></i>
                <h3 class="mb-3">Link được bảo vệ</h3>
                <p class="text-muted mb-4">
                    Link "<strong><%= link.name %></strong>" yêu cầu mật khẩu để truy cập.
                </p>
                
                <form action="/<%= shortPath %>" method="GET">
                    <div class="mb-3">
                        <input type="password" class="form-control" name="pwd" placeholder="Nhập mật khẩu" required autofocus>
                    </div>
                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-key me-2"></i>Mở khóa
                        </button>
                    </div>
                </form>
                
                <div class="mt-3">
                    <small class="text-muted">
                        <i class="fas fa-shield-alt me-1"></i>
                        Link này được bảo vệ để đảm bảo bảo mật
                    </small>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
